<?xml version="1.0"?>
<ruleset name="Dynamic Web Lab Vibe Test Plugin">
    <description>WordPress Coding Standards for Dynamic Web Lab Vibe Test Plugin</description>

	<!-- Set the memory limit to 1G.
		 For most standard PHP configurations, this means the memory limit will temporarily be raised.
		 Ref: https://github.com/squizlabs/PHP_CodeSniffer/wiki/Advanced-Usage#specifying-phpini-settings
	-->
	<ini name="memory_limit" value="1G"/>

	<!-- Strip the filepaths in reports down to the relevant bit. -->
	<arg name="basepath" value="./" />

	<!-- Check up to 8 files simultaneously. -->
	<arg name="parallel" value="8" />

	<!-- Show progress, show the error codes for each message (source). -->
	<arg name="colors"/>
	<arg value="ps" />

	<!-- Config -->
	<arg name="tab-width" value="4" />
	<arg name="extensions" value="php"/>
	<arg name="report" value="full"/>
	<arg name="report" value="summary"/>
	<arg name="report-file" value="phpcs-summary.txt"/>

    <!-- Required version -->
    <config name="testVersion" value="7.4-"/>
	<config name="minimum_supported_wp_version" value="6.0-"/>
	<config name="text_domain" value="dwl-vibe-test"/>

    <!-- Paths to check -->
	<file>.</file>

    <!-- Exclude third-party and build folders -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/build/*</exclude-pattern>
	<exclude-pattern>/.github</exclude-pattern>
	<exclude-pattern>/assets/*</exclude-pattern>
	<exclude-pattern>/languages/*</exclude-pattern>

    <!-- Include WordPress Coding Standards -->
    <rule ref="WordPress">
        <!-- Exclude FileName check to allow PSR-4 CamelCase filenames -->
        <exclude name="WordPress.Files.FileName"/>
        <exclude name="Universal.Arrays.DisallowShortArraySyntax"/>
		<exclude name="WordPress.PHP.YodaConditions.NotYoda"/>
    </rule>

	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="dwl-vibe-test"/>
			</property>
		</properties>
	</rule>

	<rule ref="Generic.Functions.CallTimePassByReference">
		<exclude name="Generic.Functions.CallTimePassByReference"/>
	</rule>

    <rule ref="Squiz.Commenting.FileComment.Missing">
        <exclude name="Squiz.Commenting.FileComment.Missing"/>
    </rule>

    <rule ref="Squiz.Commenting.FunctionComment.Missing">
        <exclude name="Squiz.Commenting.FunctionComment.Missing"/>
    </rule>

    <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>
    <rule ref="WordPress.NamingConventions"/>
    <rule ref="WordPress.PHP"/>
    <rule ref="WordPress.WhiteSpace"/>
	<rule ref="WordPress.Security"/>
</ruleset>
